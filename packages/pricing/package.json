{
  "name": "@ethosagent/pricing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The one cache-aware LLM rate table plus estimateCost(). Consumed by every provider extension so token→USD is computed in exactly one place. Depends only on @ethosagent/types.",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts",
      "production": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts"
  },
  "dependencies": {
    "@ethosagent/types": "workspace:*"
  }
}
